# Patient Influx Prediction System — Dependencies
# Install with: pip install -r requirements.txt
#
# GPU acceleration (optional — install the one matching your system):
#   NVIDIA CUDA:  pip install xgboost[cuda]  lightgbm --extra-index-url https://pypi.nvidia.com
#   Apple Metal:  scikit-learn uses Accelerate by default on macOS
#   CPU only:     all packages below use multi-core automatically

scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# Optional — significantly faster training (install if available)
# xgboost>=2.0.0
# lightgbm>=4.0.0
# torch>=2.0.0        # for GPU-accelerated custom models

# Utilities
colorama>=0.4.6       # coloured terminal output (Windows fix)
tqdm>=4.65.0          # progress bars
